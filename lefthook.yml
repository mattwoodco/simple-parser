pre-commit:
  parallel: true
  jobs:
    - name: biome-check
      glob: "*.{js,ts,jsx,tsx,json}"
      run: bunx biome check --write {staged_files}
      stage_fixed: true

pre-push:
  jobs:
    - name: type-check
      run: bunx tsgo --noEmit
    - name: biome-ci
      run: bunx biome ci .
